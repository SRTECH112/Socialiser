export type RegisterResult = {
  error?: string;
  success?: string;
};

export type RoomActionState = {
  error?: string;
  success?: string;
};

export type MessageActionState = {
  error?: string;
  successId?: string;
};
