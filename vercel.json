{
  "version": 2,
  "buildCommand": "prisma generate && prisma migrate dev --name init --create-only && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "build": {
    "env": {
      "NEXT_PUBLIC_APP_URL": "https://your-app.vercel.app"
    }
  }
}